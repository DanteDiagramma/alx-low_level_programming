#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 Success
 */
int main(void)
{
	int numbers;

	for (number = 0; numbers < 10; numbers++)
		printf("%d", numbers);
	printf("\n");
	return (0);
}
